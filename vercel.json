{
  "buildCommand": "npm install && npm -C ../shared install && npm -C ../shared run build && npm run build",
  "outputDirectory": "client/dist",
  "framework": "vite",
  "routes": [
    {
      "src": ".*",
      "dest": "/index.html"
    }
  ]
}
